# 📋 สรุปสถานะ — ส่งต่อแชทใหม่

> อัปเดตล่าสุด: 2 ส.ค. 2026 · v6.375 · commit `a6fcb59`
> อ่านไฟล์นี้ก่อน แล้วค่อยดู `CLAUDE.md` → `docs/CHANGELOG.md` (บนสุด) ตามลำดับ

## สถานะปัจจุบัน

- **v6.375** · repo สะอาด local = remote · เทสต์ **26 ชุด ผ่านหมด** · `node tools/check-order.js` ผ่าน
- บอทรันบน **VPS 185.145.158.204** (คนละเครื่องกับที่แก้โค้ด)
- ผู้ใช้อัปเดตเองผ่าน Tampermonkey ทุกครั้งที่ bump `@version`

## ✅ งานที่ปิดแล้วในรอบนี้ (v6.356 → v6.375)

| กลุ่ม | สิ่งที่แก้ |
|---|---|
| 🔴 **TDZ** | `snapBossName` ใช้ก่อนประกาศ → **ไฟต์บอสล่มทั้งรอบ 6 เวอร์ชันติด** (v6.355–6.360) · แก้ v6.361 + สร้าง `tools/check-order.js` |
| ⏱️ **นาฬิกาบอส** | ตัวนับถูกปัดทิ้งวินาที → ตรึงเป็นเวลาสัมบูรณ์ + เรียนตารางจากของจริง (v6.362/6.363) |
| 🚪 **เดินทาง** | A\* เข้าถ้ำไม่ได้ตอนประตูล็อก (เสีย 73 วิ/ทริป) → ตัดวงจรที่ ~12 วิ (v6.364) · หน้าประตูสลับกลยุทธ์รัว 3 ครั้ง/วินาที (v6.365) |
| 🎯 **เหยื่อจุดอ่อน** | ทายผิดโดยโครงสร้าง (คะแนนเสมอ → เลือกขั้นต่ำสุดเสมอ) → ถ่วงน้ำหนักด้วยเจอบ่อย+เพิ่งเจอ (v6.366) · `cycleTo` หมุนตาบอด 9 ครั้ง (v6.366) |
| ✂️ **คูลดาวน์** | ทริปที่ล้มได้คูลดาวน์เต็ม 10 นาที → บล็อกรอบถัดไปทั้งรอบ (v6.368) |
| ⚡ **charge** | ไม่เคยเดินเข้าหาบอส + เมินป้าย "ไกลไป" → 32 vs 596 dmg/ชาร์จ (v6.369) |
| 🎓 **XP/เลเวล** | กติกา XP จริง + ตัวอ่านแถบ XP + วัดอัตราจริง + ETA (v6.370–6.375) |

**ผลที่วัดได้:** ไฟต์บอส 00:11 (2/8) ได้ **14.2% — ดีที่สุดเท่าที่เคยทำ** · ได้เศษบอส 6 ชิ้น · ตาย 0

## 🟡 รอข้อมูลรอบบอสถัดไป (ไม่ต้องแก้โค้ด)

1. **`/baitbtn`** → โครง DOM จริงของ **ปุ่มเหยื่อ 2 ปุ่มในถ้ำบอส** (ผู้ใช้ส่งภาพยืนยันแล้วว่ามี 2 ปุ่ม)
   ⇒ `baitButton()` คืนปุ่มแรกเสมอ อาจอ่านขั้นผิด — **v6.367 ใส่ตัวส่องไว้แล้ว ยังไม่แก้การเลือกปุ่ม (ห้ามเดา DOM)**
2. **v6.366** ติดเหยื่อจุดอ่อนถูกเองไหม โดยผู้ใช้ไม่ต้องคลิก
3. **v6.368** ออกเดินทางเองตรงเวลาไหม (ก่อนหน้านี้ผู้ใช้ต้องกด "ล่าบอสด่วน" เอง)
4. **v6.369** ถ้าเจอบอส charge (มังกรน้ำแข็ง / นางพญาบัวสาป) — เดินเข้าหาแทนชาร์จทิ้งไหม
5. **`/gap`** สะสมเพิ่ม — ข้อมูลชุดแรกชี้ว่า **"เดินเข้าหา(ไกลไป)" = 87% ของเวลาที่เสีย** (ไม่ใช่การหลบ AoE)

## 🔴 ปัญหาที่รู้แล้ว ยังไม่ได้แก้

**หน้าเกมโหลดใหม่เอง ~9 ครั้ง/ชม.** (บางคู่ห่างกันแค่ 60 วินาที · ไม่ใช่การอัปเดตของผู้ใช้)
- ผลเสียยืนยันแล้ว: สถิติรอบขาดตอน · ตัวจับเวลาที่อิง `performance.now()` ถูกรีเซ็ตทุกครั้ง
- ถ้าไปตรงกับรอบบอส = **เสียทั้งรอบ**
- แนวทาง: ไล่ `tokpla_reload_log` + log ก่อนรีโหลดแต่ละครั้ง หาสาเหตุร่วม

## ⛔ กฎที่ต้องรู้ก่อนแตะอะไร

1. **เช็คจากเกมสด ห้ามเดา** — เปิด **แท็บใหม่** เสมอ (`tabs_create_mcp`) แล้ว navigate ไป `/api/config`
   · **ห้าม navigate ทับแท็บที่อยู่ที่ `/play`** = แท็บบอท (เคยพลาด บอทตาย กู้เองไม่ได้)
   · **ห้ามเปิดแท็บช่วงใกล้เวลาบอส/ระหว่างไฟต์** (อาจดึงโฟกัส → เทสต์คุมตัวละครไม่ผ่าน → ครบ 3 ครั้งข้ามรอบ)
2. **"ตรวจอีกรอบ" = รันเครื่องมือ** ไม่ใช่อ่าน diff — `node --check` + **`node tools/check-order.js`** + ชุดเทสต์
3. **ฟีเจอร์ที่เก็บข้อมูลเอง ยังไม่เสร็จจนกว่าจะเห็นข้อมูลจริงบน VPS** (พลาดมาแล้ว 3 รอบ)
4. **shell กิน backtick** — เขียน CHANGELOG ด้วยไฟล์ `.py` (`open(p,'wb')`) เท่านั้น อย่าพิมพ์ python ใน bash
   · และอย่าใช้ `io.open(p,'w')` กับไฟล์โค้ด — Windows แปลงทั้งไฟล์เป็น CRLF

## 🔵 งานที่ยังไม่เริ่ม

- **ปุ่มเหยื่อ 2 ปุ่ม** → แก้ `currentBait()` ให้อ่านปุ่มที่ใส่อยู่จริง (รอข้อมูลจากข้อ 1 ข้างบน)
- **รีโหลดเอง 9 ครั้ง/ชม.** (ดูหัวข้อ 🔴)
- ตัวเลือกที่ผู้ใช้ยังไม่ตัดสิน: `bossLeadMin` 3 → 5 (แนะนำให้ขยับ · `waitSec` เคยเหลือแค่ 10 วินาที)

## คำสั่ง Telegram ที่เพิ่มในรอบนี้

`/gear` · `/gap` · `/bosstime` · `/baitbtn` · `/xp` · `/xpnow` · `/xpprobe`
