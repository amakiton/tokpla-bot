# 📋 สรุปสถานะ — ส่งต่อแชทใหม่

> อัปเดตล่าสุด: 14 ส.ค. 2026 · v6.425 · ✅ watchdog ได้ผลจริง (downtime 0%) · ✅ เวลา 90% ใช้ตกปลาแล้ว · 🔴 เหลือคันโยกเล็ก (ร้าน 7%)
> อ่านไฟล์นี้ก่อน แล้วค่อยดู `CLAUDE.md` → `docs/CHANGELOG.md` (บนสุด) ตามลำดับ

## ✅ จบแล้ว: melee "ปาระเบิด" (v6.418) พิสูจน์สด → "ไม่คุ้ม" → ปิด (v6.419)

เช็คสด 13 ส.ค. (อ่าน `tokpla_boss_stats` บอท) — ไฟต์ melee หลัง v6.418 = หมึกยักษ์ท่าเรือ รอบ 01:47:
`charges 0→75` (ท่ากดค้างยิง gesture จริง) · `deaths 0` · **แต่ดาเมจสุทธิไม่ขึ้น** — เทียบบอสตัวเดียวกัน 3 ไฟต์
ระเบิด 8,570 dmg/นาที = กลางช่วงแตะล้วน (8,368–8,816) เป๊ะ = เพิ่ม 0

🔑 **ทำไม (ค่าจริง `/api/config` → `raidBomb`):** ระเบิด ×2.6 กินเหยื่อ 3 = **1.73 หน่วย/เหยื่อ** แพ้แตะจุดอ่อน ×2 = **2.0/เหยื่อ**
⇒ เมื่อเหยื่อคือคอขวด (ตุน ~300 · ไฟต์ใช้ ~423) ระเบิดเปลืองเหยื่อโดยดาเมจไม่เพิ่ม · raidBomb เป็นกลไกกลาง = สรุปนี้ใช้กับบอส melee ทุกตัว
⇒ เจ้าของสั่ง `/meleebomb off` บนบอทสด · v6.419 default OFF (โค้ดระเบิดยังอยู่ครบ · เปิดกลับ `/meleebomb on` ได้)
⚠️ **บทเรียนวิธีการ: "ดูบอทสู้สด" ทำไม่ได้** (เกม single-session · เปิด `/play` ตอนรอบบอส = เตะบอทตาย) — วิเคราะห์จาก stats ที่บอทจดเท่านั้น
· ทางต่อยอด (ถ้าอยากไล่ 2.6× จริง): "ระเบิดเฉพาะตอนเหยื่อล้น" + ตัววัด — เก็งกำไรสวนข้อมูลปัจจุบัน ยังไม่ทำ

## ✅ watchdog ติดตั้งแล้ว — ได้ผลจริง (13 ส.ค. 17:22)

ติดตั้งบน VPS สำเร็จ (Scheduled Task `TokplaWatchdog` = Running · Edge Sleeping Tabs ปิดที่ policy)
**หลักฐาน:** ช่องเงียบ >8 นาที ตั้งแต่เที่ยง–23:22 มี **ครั้งเดียว = 16:24→17:18 (53.8 นาที · ก่อนติดตั้ง)**
· หลังติดตั้ง **6 ชม. ศูนย์ครั้ง** · ครั้งที่ตายนั้นทำให้พลาดบอส 16:30 เต็มรอบ = ความเสียหายที่ watchdog มีไว้กัน
· ตรวจ: `Get-Content "$env:LOCALAPPDATA\tokpla-watchdog\watchdog.log" -Tail 20` (เงียบ = ดี) · ปิดฝั่งบอท `/watchdog off`

## 🔴 งานด่วนสุดของแชทใหม่: พิสูจน์ A/B ชาร์จรอบใหม่ (v6.421)

**เจอบั๊กที่ทำให้การทดลองนี้ไร้ค่ามา 60 เวอร์ชัน:** `tokpla_charge_ab` มี 18 บล็อก — **แขน tap 18 · full 0**
ต้นเหตุ: เส้นแบ่ง act/gap (`GAB_TICK_MAX` 1500ms) **สั้นกว่าการชาร์จ 1 ครั้ง** (~1,950ms)
⇒ ทุกจังหวะของแขน full ถูกนับเป็น "ช่องว่าง" ⇒ โดนตัดทิ้งทุกบล็อก ⇒ **อคติเชิงระบบ ไม่ใช่ข้อมูลน้อย**
⇒ ผลคือ charge (โหมดที่อ่อนสุด **4.7%** vs cast 10.5% · melee 10.1%) **ใช้ท่าที่ไม่เคยวัดเลย**

v6.421 แก้: `chabTickMax() = BOSS_CHARGE_MS + 900` (อิงค่าจริง ตามเกมเอง) + จดสาเหตุทิ้งแยกรายแขน + รีเซ็ตข้อมูลเก่าครั้งเดียว

**ต้องทำ:** รอไฟต์ charge 2-3 รอบ (มังกรน้ำแข็ง/นางพญาบัวสาป) แล้วสั่ง `/chargeab`
- ✅ สำเร็จ = มีบล็อกแขน **ชาร์จเต็ม > 0** (เดิม 0 ตลอดกาล) และรายงานไม่ขึ้น `⏳ ยังไม่ครบสองแขน`
- ❌ ถ้ายัง 0 = ดูบรรทัด `บล็อกที่ถูกทิ้ง` ว่าเป็น `full/...` ด้วยเหตุใด (v6.421 แยกรายแขนให้แล้ว)
- ครบ 6 ไฟต์แล้วระบบจะเลือกแขนที่ dmg/วิ ชนะเองถาวร

## ✅ ผลจริงจาก `/time` (14 ส.ค.) — ระบบสุขภาพดีกว่าที่คิด

    🎣 ตกปลา 555 นาที 90%  ·  🏪 ร้าน/กระเป๋า 44 นาที 7%  ·  👹 ล่าบอส 20 นาที 3%
    💀 สคริปต์ไม่ได้รัน 0%   ← watchdog ทำงาน 100% (13 ส.ค. เคยตาย 54 นาทีรวด)

⇒ **เวลา 90% ใช้ตกปลาแล้ว** — เพดานของการจัดสรรเวลาเหลือแค่ ~10%
· ตัวกินเวลาอันดับ 1 ที่ไม่ใช่การตก = ร้าน/กระเป๋า 7% (ลดครึ่งได้ ≈ +3-4% cast ≈ +130k 🪙/วัน)
· 👹 ล่าบอส 3% ของเวลา แลกรายได้ ~2% = **สมน้ำสมเนื้อ ไม่ต้องแตะ**

⚠️ **v6.425 แก้บั๊กตัวเลขพอง 28%** — `/time` เดิมเอา cast ทั้งวันหารด้วยงบเวลาที่สั้นกว่า
(638/ชม. ทั้งที่จริง 497/ชม.) · ตอนนี้แยก "ในช่วงที่วัด" ให้แล้ว · **ตัวเลขก่อน v6.425 เชื่อไม่ได้**

📌 **อัตราเหวี่ยงรายวัน (ค่าจริง):** 12 ส.ค. 332/ชม. · 13 ส.ค. 527/ชม. · 14 ส.ค. 497/ชม.
⇒ กระโดดจาก 332 → ~500 มาจาก **การอุด downtime** (watchdog + ปิด Sleeping Tabs) เป็นหลัก

## ⏱️ v6.424 — คันโยกใหญ่สุดที่เหลือ: **อัตราเหวี่ยงต่างกัน 59% ระหว่างวัน**

    12 ส.ค.  7,961 cast = 332/ชม.
    13 ส.ค. 12,647 cast = 527/ชม.   ← +59% ทั้งที่เวลาเดินเท่ากันและพักพลังต่างกันแค่ 1% vs 3%

⇒ ดัน ~390 → 527 ทุกวัน ≈ **+2 ล้าน 🪙/วัน** (~14 เท่าของรางวัลบอสทั้งวัน) · **แต่ยังไม่รู้ว่าเวลาหายไปไหน**

v6.424 เก็บงบเวลารายวัน (สุ่มสถานะทุก 30 วิ · ไม่แตะพฤติกรรม): `tFish` `tMenu` `tBoss` `tRest` `tOff` `tDead`
· `tDead` = ตัวจับเวลาขาดเกิน 2.5 นาที = แท็บตาย/รีโหลด ⇒ **downtime โผล่ในบัญชีเอง**

🔴 **งานถัดไป — สั่ง `/time` (มีข้อมูลหลังบอทเดิน ~1 ชม.):**
1. ดูว่าตัวไหนกินเวลามากสุดที่ **ไม่ใช่** การตกปลา — รายงานชี้เป้าให้พร้อมคำแนะนำ
2. เทียบ `ตอนตกจริง N เหวี่ยง/ชม.` กับค่าเฉลี่ยรวม — ถ้าตอนตกจริงสูงแต่รวมต่ำ = **ปัญหาคือเวลาที่อื่น ไม่ใช่ความเร็วตก**
3. `tDead` ควรเป็น ~0% ถ้า watchdog ทำงาน (ตัวตรวจสอบ watchdog ในตัว)

## ✅ v6.423 — พลังงาน **ไม่ใช่** คอขวด (ปิดประเด็น)

`/energy` ผลจริง: นั่งพัก **2%** ของเวลาเดิน (เกณฑ์คอขวด ≥15%) · เบ็ดที่ใส่อยู่ (แมกนีเซียม) ดีสุดทั้ง 3 แกนแล้ว
(ปลา 39 · ประหยัดพลัง 9.9% · เร็ว +16%) ⇒ **ไม่ต้องแก้ตรรกะเลือกเบ็ด** · สมมุติฐาน "+550k/วัน" ตกไป
📌 คุ้มที่วัดก่อน — ถ้าเชื่อตัวเลขบนกระดาษแล้วรีบแก้ = เพิ่มความซับซ้อนโดยไม่ได้อะไร (แพตเทิร์นเดียวกับ melee ปาระเบิด)

## ⚡ v6.423 — คันโยกที่ใหญ่ที่สุดที่ยังไม่ได้แตะ: **พลังงาน**

**ตรรกะ:** รายได้ 98% มาจากฟาร์ม (บอสให้ ~2% ดู v6.422) ⇒ ตัวคุมรายได้ = **จำนวน cast/วัน**
· log สดบอกว่าบอทติดคอขวดพลัง (`🩺 ไม่ได้เหวี่ยงเบ็ดเลย 15-20 นาที — นั่งพักรอพลังฟื้น`)
· 🔴 **แต่ `rodDetail()` ไม่เคยอ่าน "ประหยัดพลัง" เลย** (ให้คะแนนจาก แรร์/เงาวับ/โชค/บอส เท่านั้น)
· เบ็ดคาร์บอนถักมี `ประหยัดพลัง 8.8%` ⇒ ≈ cast +8.8% ≈ **+550k 🪙/วัน** (ใหญ่กว่ารางวัลบอสทั้งวัน ~4 เท่า)

**v6.423 ทำแค่ "วัด" ยังไม่เปลี่ยนการเลือกเบ็ด** (บทเรียน melee ปาระเบิด: ต้องวัดก่อนเชื่อ)

🔴 **งานถัดไป — สั่ง `/energy` แล้วดู 2 ตัวเลข:**
1. `นั่งพัก % ของเวลาเดิน` — **≥15% = พลังเป็นคอขวดจริง** · <15% = ไล่เรื่องนี้ไม่คุ้ม
2. บรรทัดเทียบเบ็ด: `cast/วัน +X%` vs `มูลค่า/cast ±Y` — ถ้าเป็น "แลกกัน" ต้อง A/B จริงก่อนสลับถาวร
   (เบ็ดประหยัดพลังมักมีแรร์/เงาวับต่ำกว่า = แลกจำนวนกับมูลค่า)
· ตารางเบ็ดจะว่างจนกว่าบอทจะสลับเบ็ดครั้งถัดไป (เก็บผลสแกนอัตโนมัติ v6.423+)

## 🔇 v6.423 — log ถูกสแปมกิน 51% (แก้แล้ว)

`ป้าย "ถึงรอบบอสแล้ว"` ซ้ำ 772/1,500 บรรทัด ⇒ ring log ครอบคลุมแค่ 209 นาที
· ต้นเหตุ: ธงกันสแปม (v6.169) **ถูกรีเซ็ตทุกครั้งที่สแกนไม่เจอป้าย** + ป้ายเกมกะพริบ = log ใหม่ทุก ~16 วิ
· แก้เป็น throttle ตามเวลา 5 นาที (+log ทันทีเมื่อเหตุผลเปลี่ยน) ⇒ เทสต์: −95% · **ประวัติวินิจฉัยยาวขึ้น ~2 เท่า**
· 📌 เรื่องนี้เกือบทำให้ไล่เคส "แท็บตาย 16:24" กับ "พลาดรอบ 20:00" ไม่ทัน — log ที่ยาวพอคือเครื่องมือวินิจฉัยหลัก

## 🎁 v6.422 — รางวัลบอสเคยถูกผูกมั่ว (แก้แล้ว · รอยืนยันสด)

`/bossstats` เดิมโชว์ไฟต์ 11/08 19:35 ได้ `515,100 🪙` = **63% ของยอดรวมทั้งตาราง** — ค่าดิบคือ `mails: 20`
⇒ บอทเปิดกล่องจดหมายทีเดียว 20 ฉบับแล้ว**บวกรวมทั้งกองใส่ไฟต์ล่าสุด** (รางวัลจริงของไฟต์นั้น ~25,755)

v6.422 แก้: อ่าน **ชื่อบอส + วันเวลา + เลขดาเมจ** ที่มีอยู่ในตัวจดหมาย → ผูกทีละฉบับกับไฟต์ที่ตรงกัน
· จับคู่ไม่ได้ = ลงบัญชี "ค้างเก่า" แยก (เงินนับครบ ไม่บิดสถิติ) · ตารางขึ้น `*N` เมื่อรวมหลายฉบับ

**ต้องดู:** หลังรอบบอสถัดไป สั่ง `/bossstats` — คอลัมน์ coin ควร**ไม่มี `*N`** (ผูกได้ 1:1) และมีบรรทัด "เฉลี่ย X 🪙/ไฟต์"

📌 **สูตรรางวัล (จาก 10 ไฟต์ที่ข้อมูลสะอาด):** ≈ **1,250 🪙 ต่อส่วนร่วม 1%** + ฐาน ~4,400 · เฉลี่ย 14,369/ไฟต์
🔴 **แต่บอสเป็นเงินก้อนเล็ก:** ~144,000 🪙/วัน เทียบกำไรฟาร์ม ~6.28 ล้าน/วัน = **~2%**
⇒ คันโยกดาเมจบอสให้ผลเป็นเหรียญน้อยมาก · **คุณค่าจริงอยู่ที่ของ** (เศษบอส/เบ็ด/ทุ่น/คอสตูม/เหยื่อแรร์)
⇒ ใช้ตัดสินใจจัดลำดับงาน: ปรับปรุงการฟาร์ม 1% > ปรับปรุงดาเมจบอส 50%

## 📊 สถานะข้อมูลทดลองอื่น (เช็คสด 13 ส.ค. 23:22)

- ✅ **v6.408 (ตัวขยับสมอ) ยิงจริงแล้ว** — ไม่ใช่ "ยังไม่เคยทำงาน" อีกต่อไป:
  `12/08 01:45 เป๊ะ17% recenter2` · `12/08 13:32 เป๊ะ16% recenter2` · `13/08 00:17 เป๊ะ35% recenter3`
  ⇒ ยิงถูกจังหวะ (เฉพาะไฟต์ที่สมอปักพลาด) **แต่ยิงแล้วเป๊ะยังไม่ขึ้น** (เทียบไฟต์ที่สมอปักดีเอง = เป๊ะ 81-96%)
  ⇒ มีข้อมูลพอสรุปว่า **ยังไม่แก้ปัญหา แต่ไม่ทำให้แย่ลง** · ถ้าจะตัดออกให้ `/beatcenter off`
- ⚪ melee/charge อ่านป้ายจังหวะไม่ออก 100% = **ปกติ** (2 โหมดนั้นไม่มีระบบบีต) · cast อ่านได้ 98%
- ✅ ฟิลด์สถิติพื้นฐานครบ 20/20 ไฟต์ (โหมด/บอส/จุดอ่อน/เหยื่อ/ดาเมจ/ส่วนร่วม/ตาย/คอมโบ)

## ✅ คันโยกทำคะแนนบอสที่ทำเสร็จแล้ว

**🥁 โหมดกด cast = hybrid ±140 (v6.415) — ยืนยันแล้ว สถิติใหม่:**

    บอสเดียวกัน (พญาบึกทองคำ) ดาเมจ/นาที:
    ล็อกแคบ ±70 (เดิม): 4,638 · สาดรัว: 10,899 · hybrid ±140: 13,680 ← สูงสุด (เป๊ะ 81% + กด 215/นาที)

⇒ เกมให้ "เป๊ะ" ถึง ±150ms แต่บอทล็อกแค่ ±70 (แคบครึ่ง) · **เก็บ `/beatgate on` + `beatWin 140` ไว้**
· จูน `/beatwin N` (40-150) · ดู [[tokpla-boss-perfect-is-beat]]

**🎣 เบ็ดตีบอส (v6.417) — 2 เรื่อง:**
- ✅ **เบ็ดบอสตันแล้ว** — บอทเลือกดุกนรก (บอส27%=ดีสุด) ถูกอยู่แล้ว · ข้อจำกัด v6.188 หายไปตั้งแต่ย้ายมาใช้การ์ด
- 🔴 **เจอบั๊กเสียเงิน:** เกมเลิกใช้ "โบนัสปลา" → ทุกเบ็ดอ่าน "ปลา–" → **ฟาร์มด้วยเบ็ดบอสตลอด** (แก้แล้ว)
  แผงจริงตอนนี้: `โอกาสแรร์ +40% (ได้จริง 6%) · เงาวับ +20% · โชคปลาแรร์ +10%` · ยืนยันสด: อ่านแรร์/เงาวับได้แล้ว

**⚡ ตุนพลังก่อนบอส (v6.414):** `bossEnergyTarget 55` — 0 ตายหลายไฟต์ · departure พรีเอมต์ = ไม่พลาดบอส
**🎯 เหยื่อจุดอ่อน:** ตัวคูณสูงถึง **×2** (ไม่ใช่แค่ 1.5) · ตุนครบทุกไฟต์ · คอมโบ≥6 · วาร์ป · uptime ครบ

**สูตรคะแนน (จาก /api/config):** เป๊ะ 0.88 · ดี 0.67 · หลุด 0.35 · ไม่มีบีต 0.86 · เพดาน 6,000/ตี · combo +30% ที่ 6

## ⚙️ คอนฟิกที่ล็อกไว้ (ค่าดีที่สุด · อยู่ใน saved cfg + DEFAULTS แล้ว)

    beatGate: on (ล็อก/hybrid) · beatWin: 140 · meleeBomb: off (v6.419 พิสูจน์แล้วไม่คุ้ม) · bossEnergyTarget: 55 · bossWarp: on
    เหยื่อฟาร์ม: ขั้น 11-12 (เลือกถูกแล้ว ดู [[tokpla-profit-is-jackpot-driven]])

## 🧵 เกร็ดที่ถามระหว่างทาง

- **ชุดคอสตูม:** หมึกยักษ์ 5/5 (+6% น้ำหนัก/+3% ดวง) > ขอทาน (+5%/+2%) **ถ้าใส่ครบ** · บอทฟาร์มที่ท่าเรือทะเล = เงื่อนไข location ผ่าน · ตอนนี้หมึกยักษ์แค่ 4/5 (+4%/+2%) แพ้ขอทาน → หาชิ้นที่ 5 ก่อน

## 🔴 เสถียรภาพ — "แท็บตาย" ยังเกิดซ้ำ · watchdog คือทางแก้ (รอเจ้าของตัดสิน)

**อาการที่แก้ได้แล้ว:**
- 🐟 หยุดเพราะกดตกปลาไม่ติด → v6.413 กู้อัตโนมัติ (เพดาน 5 ครั้ง/15 นาที)

**อาการที่ยังเกิดซ้ำ (แก้จากสคริปต์ไม่ได้):**
- 💀 **แท็บตาย** — hb ค้าง · `enabled=1` (ไม่ได้หยุดเอง) · แท็บ/tab group หายจาก session · เกิดหลายรอบ
  (เช่น 13 ส.ค. 00:20 ดับ ~13 นาที · น่าจะพลาดบอส 00:13) · เจ้าของเคยว่า "VPS ปิดเอง" แต่**ยังเกิดซ้ำ**
  ทุกครั้งต้อง navigate `/play` ใหม่ → บอท auto-resume (enabled=1 + resumeFlag=1)
  🔑 **ตัวแก้จริงมีทางเดียว = watchdog นอกเบราว์เซอร์** (แท็บตายแล้วกู้ตัวเองไม่ได้ ไม่มีสคริปต์รัน)
  → เสนอไว้แล้ว: สคริปต์ PowerShell + Task Scheduler บน VPS เช็ค hb ทุก ~10 นาที ค้าง=รีโหลด
  → **เจ้าของยังไม่ตัดสิน** · เป็นตัวขวางเป้าหมายที่ใหญ่สุด (คันโยกทำคะแนนไร้ค่าถ้าบอทดับครึ่งวัน)

**วิธีเช็ค+กู้ (ทำด้วยมือตอนนี้):** เปิดแท็บใหม่ → api.ipify.org ต้อง 185.145.158.204 → navigate `/api/config`
อ่าน `tokpla_bot_hb` (ค้าง = ตาย) → navigate `/play` รอ ~40 วิ (โหลดช้า) → บอท resume เอง

## 🔁 v6.413 — กู้บอทอัตโนมัติ (แก้อาการ "ดับข้ามคืน 15.6 ชม.")

**เหตุการณ์ 10 ส.ค.:** บอทหยุดเอง 19:37 หลังไฟต์บอส charge (กดตกปลาไม่ติด → รีเฟรช → ยังไม่หาย → หยุด)
→ **ดับยาว 15.6 ชม.** จนคนมากดเปิดตอน 11:14 · พอเปิดใหม่ตกปลาได้ทันที = อาการเป็น **ชั่วคราว**

🔑 **กุญแจ:** หน้าเว็บโหลดใหม่เองตอน 00:04 เคลียร์อาการไปแล้ว **แต่บอทไม่กลับมา** เพราะ self-stop
ล้างธง `tokpla_bot_resume` (หยุดตั้งใจ = ไม่ auto-resume) ⇒ นอนตายรอคนทั้งที่อาการหายตั้งแต่เที่ยงคืน

**v6.413 แก้:** หยุดเพราะ `กดตกปลาไม่ติด` → เปิด episode "กู้อัตโนมัติ" · `heartbeatWatch` ลองรีโหลด+เปิดใหม่
ทุก ~15 นาที สูงสุด 5 ครั้ง (~75 นาที) · ตกได้จริง = กู้สำเร็จล้าง episode · เหตุอื่น (login/เงินหมด) = ไม่กู้
· เช็คจาก marker ใน localStorage ⇒ **กู้ได้แม้หน้าเว็บโหลดใหม่เอง** · ไม่แตะตรรกะการตี · เทสต์ 40 เคสผ่าน

🔴 **ต้นเหตุที่ยังไม่ได้แก้:** ทำไมกดตกปลาไม่ติดหลังไฟต์ charge (transient · ต้องเจอสดถึงเจาะได้)
· v6.413 แก้ที่ "ผลลัพธ์" (ไม่ให้ดับยาว) ไม่ใช่ที่ราก

## 📊 v6.412 — ตัวนับ "ก่อน/หลังล็อกบีต" (กำลังเก็บข้อมูล)

ข้อมูล 10 ไฟต์ cast ชี้ว่า **ดาเมจ/นาที ผูกกับ "อัตราการกด" (r=0.62) มากกว่า "เป๊ะ%" (r=0.30)** —
`beatOkNow()` บีบให้กดเฉพาะ ±70ms หลังล็อก ⇒ ล็อกได้ = กดน้อยลงเป็นเท่าตัว (ไฟต์ 13:32 กดแค่ 25/นาที)
· v6.412 เพิ่ม `beatLockAtMs`/`gaugePreLock` เพื่อเทียบ **ก่อน vs หลังล็อก ในไฟต์เดียวกัน** (ตัดตัวแปรออก)
· **ต้องทำ:** เก็บ 2-3 ไฟต์ cast แล้วดูว่าล็อกบีตคุ้มไหม · ถ้าไม่คุ้ม → ทำสวิตช์ `/beatgate` A/B เทสต์

## ✅ v6.411 — ตาบอดป้ายจังหวะ (ยืนยันแล้ว: 26% → 5%)

ป้ายผลจังหวะเป็น "ป้ายชั่วคราว" · เดิมอ่านครั้งเดียวที่ 260ms แล้วทิ้ง → รอจนเห็น/หมดหน้าต่าง 600ms
· ไฟต์ 00:11 tapNoFb 26% · ยังไม่ได้เทสต์ v6.411 บนไฟต์ cast ที่สมอปักพลาด (00:11 สมอปักดีเอง)

## ✅ จบแล้ว — บอทกลับมาฟาร์มได้ (v6.409 + v6.410) · ยืนยันสด 9 ส.ค. 22:49

อาการเดิม "ซื้อเหยื่อไม่ได้ → บอทหยุดเอง" มี **ต้นเหตุ 2 ชั้นซ้อนกัน** ต้องแก้ทั้งคู่ถึงจะหาย:

| ชั้น | ต้นเหตุ | แก้ |
|---|---|---|
| 1 | เกมเรนเดอร์รายการ **ทีหลัง** (กรอบ 150ms · รายการ ~465ms) แต่บอท `sleep(300)` ตายตัวแล้วอ่านเลย ⇒ เห็น 0 แถว | **v6.409** รอเงื่อนไขแทนเวลา |
| 2 | เกมเพิ่ม **หน้าต่างยืนยันก่อนตัดเงิน** · ตอนเปิดมีปุ่ม "ซื้อเลย!" **2 ตัว** และ `btnByText` คืนตัวตะกร้าเสมอ ⇒ กดวนไม่มีวันสำเร็จ | **v6.410** หาปุ่มยืนยัน "จากในกล่อง" + รวมจุดซื้อ 7 จุดเป็น `buyResult()` |

**ยืนยันสด:** `22:49:53 ซื้อ… → 22:49:55 ✅ ซื้อ 1 แพ็ค (100 ชิ้น · 25,100🪙)` (2 วิ · เดิม timeout 8 วิ)
· หลังจากนั้น **ล้มเหลว 0 ครั้ง** · ตกปลาได้ 35 ตัว/30 นาที · รีโหลดถี่หยุดแล้ว (เป็นช่วงอัปเดตสคริปต์)

📌 **`tk-inner` และ selector ทั้งหมดไม่เคยเปลี่ยนเลย** — ที่เปลี่ยนคือ *จังหวะ* กับ *ขั้นตอน* เท่านั้น

## 💰 วิเคราะห์เศรษฐศาสตร์ (9 ส.ค.) — ขั้น 11 ที่ใช้อยู่ "เลือกถูกแล้ว" อย่าเปลี่ยน

ตลอดชีพ: รายได้ **321** 🪙/cast − เหยื่อ **162** = **+159/cast** · กำไรสะสม **+12.36 ล้าน** (79,452 cast)

🔴 **กำไรเกือบทั้งหมดมาจากปลาแรร์ 5% บนสุด** — ตัด 5% บนออกแล้ว **รายได้ต่อ cast ติดลบทุกขั้น**:

    ขั้น   ราคาเหยื่อ  เฉลี่ยดิบ  สุทธิดิบ   ตัดโชค5%   สุทธิหลังตัด   รายได้จาก5%บน
     9       220        568      +348       154        −66          74%
    10       236        345      +109       156        −80          57%
    11       251        312       +61       209        −42          36%   ← ใช้อยู่

⚠️ **กับดักที่เกือบทำให้ตัดสินใจผิด:** ค่าเฉลี่ยดิบบอกว่าขั้น 9 ชนะขาด (+348 vs +61)
**แต่มาจากปลาตัวเดียวราคา 99,569** (= 249/cast จาก 400 ตัวอย่าง!) — ถ้าเชื่อแล้วสั่งถอยไปขั้น 9
= ตัดสินใจจากปลาตัวเดียว · พอตัดโชคออก **ขั้น 11 ดีที่สุดและพึ่งโชคน้อยที่สุด (36%)**
⇒ **ไม่ต้องเปลี่ยนขั้นเหยื่อ** · ถ้าจะเปลี่ยนจริงต้องทดสอบแบบคุมตัวแปร ไม่ใช่เทียบข้อมูลคนละวัน
· ดู [[tokpla-profit-is-jackpot-driven]]

## 🐯 ไฟต์บอส 10 ส.ค. 00:11 — ดาเมจนิวไฮ 34.5% (เดิม 24.3%) · แต่ v6.408 **ยังไม่ถูกทดสอบ**

**พรายทองเจดีย์จม** (สาย cast) · `sunken_stupa` · ฆ่าได้ · ตาย 0 · ใช้เวลา 2:43

    เป๊ะ 83 · ดี 1 · หลุด 1  ⇒ เป๊ะ 98%   (baseline รวมของเก่า 287 กด = 49%)
    คอมโบสูงสุด 43 · ดาเมจ 34,546 = 34.5%  ← สถิติใหม่ (เดิม 24.3%)
    วาร์ปเข้าถ้ำสำเร็จ (sea_dock → sunken_stupa) · กดตีครั้งแรกหลังบอสโผล่ 3.8 วิ
    จุดอ่อน จิ้งหรีด+สปินเนอร์ → ใช้เหยื่อขั้น 3 (มี 200 ชิ้น) ได้ ×1.5 ครบ ✅

✅ **ผลตรงของการแก้ร้าน (v6.409+410):** รอบ 22:30 ก่อนหน้าไม่มีเหยื่อจุดอ่อนเลย (`ขั้น1×0 · ขั้น6×0`)
⇒ เสีย ×1.5 ทั้งไฟต์ ได้อันดับ 17 · รอบนี้ตุนไว้ครบ 6 ขั้น + ยา 3 ขวด ⇒ ดาเมจนิวไฮ

🔴 **แต่ห้ามเครดิตให้ v6.408 — กลไกไม่เคยทำงานเลยในไฟต์นี้:**

    beatRecenters: 0     ← ตัวขยับสมอของ v6.408 ไม่เคยยิง
    beatUnlocks:   0     ← ประตูหนี "ดีเยอะ" ไม่เคยเปิด
    beatLock: 1022 · beatPrecise: true · beatSrc: "เป๊ะ"   ← สมอปักครั้งเดียวแล้วดีเลย

⇒ เป๊ะ 98% มาจาก **สมอบังเอิญปักตำแหน่งดีตั้งแต่แรก** (ตรงกับทฤษฎีเดิมว่า "ไฟต์ไหน = อธิบายทุกอย่าง")
⇒ **v6.408 ยังพิสูจน์ไม่ได้** — ต้องรอไฟต์ที่สมอปักพลาด (เป๊ะเริ่มต้นต่ำ) แล้วดูว่า `beatRecenters` ขยับไหม
⚠️ **อย่าสับสน `recenters` (= เดินกลับกลางสนาม 14 ครั้ง) กับ `beatRecenters` (= v6.408)** — คนละเรื่องกันสิ้นเชิง

⚠️ **อาการตาบอดป้ายจังหวะยังอยู่:** `tapNoFb: 30` = อ่านผลไม่ออก 30 จาก 115 กด (~26%)
   ⇒ ไม่มี feedback = ล็อกใหม่ไม่ได้ + ข้อมูลหาย · ยังเป็นงานค้าง

## 🕐 v6.411 — แก้ "ตาบอดป้ายจังหวะ" แล้ว (รอพิสูจน์ไฟต์ 10:30)

ไฟต์ 00:11 กด 115 ครั้ง แต่ `tapNoFb 30` (~26%) — ต้นเหตุคือ `beatSettle()` อ่านป้ายผล **ครั้งเดียว**
ที่ 260ms หลังกดแล้วสรุปทันทีว่าอ่านไม่ออก · แต่ป้าย "เป๊ะ!/ดี/หลุดจังหวะ" เป็น **ป้ายชั่วคราว**

🔴 **สำคัญกว่าที่คิด:** ไม่มี feedback ⇒ ตัวล็อกบีต + ตัวขยับสมอ (v6.408) **ไม่มีข้อมูลให้ตัดสินใจ**
⇒ อาการตาบอดนี้คือ **ตัวที่กันไม่ให้ v6.408 ทำงาน** ตั้งแต่ต้น (แก้แล้วจึงเพิ่งมีโอกาสพิสูจน์ v6.408)

แก้: `if (!fb && now() - tapWait.at < TAP_FB_WAIT_MS) return;` (600ms · ไม่ล้าง `tapWait` = ลองใหม่รอบหน้า)
· ปลอดภัยเพราะจุดตั้ง `tapWait` ใช้ `if (!tapWait)` อยู่แล้ว · 600ms < บีต 1200ms = ไม่จับคู่ผลผิดตัว

## 📅 ตารางรอบบอส — **ค่าจริงจาก `/api/config` (แก้ 13 ส.ค. · ของเดิมจดไม่ครบ)**

🔑 **แหล่งความจริง = `raidSpawnMinutes` ใน `/api/config`** (นาทีของวัน) — ไม่ต้องเดา ไม่ต้องรวบรวมจาก log:

    [13, 103, 412, 630, 810, 912, 990, 1170, 1200, 1350]
    = 00:13 · 01:43 · 06:52 · 10:30 · 13:30 · 15:12 · 16:30 · 19:30 · 20:00 · 22:30   (10 รอบ/วัน)
    raidWindowMinutes: 60 (หน้าต่างของแต่ละรอบ) · raidBossRotation: "rotate" (บอสสุ่มต่อรอบ ดู tokpla_boss_rota)

🔴 **ของเดิมเขียนว่า "ว่างข้ามคืน · ไม่มีรอบ 01:30/03:11/07:00" — ผิด** · ยืนยันจากไฟต์จริง 13 ส.ค.:
บอทตีรอบ **01:47 · 06:55 · 15:15** (= รอบ 01:43 / 06:52 / 15:12 บวกเวลาเดินทาง ~3 นาที) ครบทุกรอบ
⇒ ความเชื่อ "ข้ามคืนไม่มีบอส" ทำให้ **มองข้ามการพลาดรอบกลางคืนว่าเป็นเรื่องปกติ** (อันตรายกว่าการไม่รู้)

⚠️ ยังจริงอยู่: ป้าย "ถึงรอบบอสแล้ว" บนจอ **เชื่อไม่ได้** (ค้างได้ · 13 ส.ค. 16:00 บอทอ่านป้ายค้างแล้ว log ซ้ำ 101 ครั้ง)
· ตอนตกปลาที่ท่าเรือจอไม่มีตัวนับให้อ่าน — บอท fallback ไปใช้ตาราง (`🔮 ... ใช้ตาราง (รอบถัดไป HH:MM)`) ซึ่งถูกต้องแล้ว

## 📊 ผลฟาร์มหลังแก้ครบ (10 ส.ค. 00:00–03:17 · 3.3 ชม.)

    ตกได้ 778 ตัว (237/ชม.) · รายได้ 464,191🪙 · เฉลี่ย 597🪙/ตัว (เหยื่อ 251 ⇒ +346/cast)
    legendary 12 · mythic 3 · epic 72 · rare 217 · ปลาแพงสุด 61,089
    กำไรสะสม 12.36 ล้าน → 13.96 ล้าน (+1.6 ล้านใน ~4.5 ชม.)

📌 เฉลี่ย 597/ตัว สูงกว่าค่าเฉลี่ยเก่าของขั้น 11 (312) เกือบเท่าตัว ⇒ **ยิ่งยืนยันว่าขั้น 11 เลือกถูก**

## 🔴 งานถัดไป (เรียงตามความสำคัญ)

1. **พิสูจน์ v6.411** — ไฟต์ 10:30: `tapNoFb` ต้องลดลงจาก 30 อย่างชัดเจน
2. **พิสูจน์ v6.408** — ต้องรอไฟต์ที่ **สมอปักพลาด** (เป๊ะเริ่มต่ำ) แล้วดูว่า `beatRecenters` > 0 ไหม
   · ไฟต์ 00:11 สมอปักดีตั้งแต่แรก กลไกเลยไม่มีอะไรให้ทำ · ถ้าแย่ลง → `/beatcenter off`
3. **เบ็ดที่ปักหมุดไม่ถูกใช้** — ข้อจำกัดเดิม v6.188 (ปุ่ม G เลือก "ชิ้น" เจาะจงไม่ได้) ไม่ใช่บั๊กใหม่
4. `⚠️ ติดเหยื่อล่วงหน้าไม่สำเร็จ (ขอขั้น 6 · ได้ 3)` — ไม่กระทบผล (จับคู่จุดอ่อนใหม่ตอนเริ่มไฟต์)

## 🔴 ความเสถียร — มี 3 อาการ (คนละเรื่อง อย่าสับสน)

| อาการ | หลักฐาน | ตัวจับ | ตัวแก้ |
|---|---|---|---|
| **2 หน้าต่างแย่ง session** (เกมค้าง 4%) | 9 ส.ค. ~5 ชม. · ปิดเหลือหน้าต่างเดียว → โหลด 100% ทันที | ข้อมูล localStorage 2 ชุดขัดกัน + ค้างโหลด | ✅ **เปิดหน้าต่างเดียว** (ต้นตอใหญ่สุด · [[tokpla-one-window-only]]) |
| **แท็บตาย** (สคริปต์หยุดสนิท) | 7 ส.ค. 6.5 ชม. · hb ค้าง · ไม่มี log/reload | v6.402 (freeze/discard → `/reloads`) | 🔴 **ต้องตั้ง Edge บน VPS** (ยังไม่ทำ) |
| **เข้าจุดเซฟไม่ได้→หยุดเอง** | 8 ส.ค. 5 ชม. · แท็บยังรัน ยิงแจ้งเตือน | v6.399 🔔 | ✅ v6.406 แก้แล้ว |

> ⚠️ ก่อนโทษ Edge sleeping tabs/discard ให้เช็ค **จำนวนหน้าต่างเกม** ก่อน — 9 ส.ค. พบว่าอาการ "ตายบ่อย" ส่วนใหญ่คือ 2 หน้าต่างแย่งกัน ไม่ใช่ discard

🔴 **เบราว์เซอร์บน VPS คือ Microsoft Edge ไม่ใช่ Google Chrome** (ยืนยัน 8 ส.ค. จาก `navigator.userAgentData.brands`
= `Microsoft Edge, Chromium` · เจ้าของยืนยันว่าใช้ Edge เปิดเกมมาตลอด) — เอกสารเดิมจดว่า Chrome **ผิด**
⇒ `chrome.exe` ไม่มีบนเครื่องนั้น · `taskkill /IM chrome.exe` ไม่แตะอะไรเลย (โปรเซสชื่อ `msedge.exe`)
⇒ **คำสั่ง/พาธ/หน้า settings ทุกอย่างต้องใช้ของ Edge** (`edge://…`) ไม่ใช่ `chrome://…`

**ผู้ต้องสงสัยอันดับ 1 ของอาการแท็บตาย = Edge "Sleeping tabs"** (ดีฟอลต์: หลับแท็บที่ไม่ได้โฟกัสหลัง 5 นาที)
หลักฐานสด 8 ส.ค. 23:20 น.: `heartbeat` ค้าง **354 วิ** (ต้องเต้นทุก 30 วิ) · log ค้างที่ 23:11:19
⇒ ช่วงเงียบ ~5.5 นาที **ตรงกับตัวจับเวลาของ Sleeping tabs พอดี**

**ตัวแก้แท็บตาย (งานเจ้าของที่ VPS — ยังไม่ทำ):**
1. `edge://settings/system` → ปิด **"Save resources with sleeping tabs"** + **"Efficiency mode"**
   (ทางเลือกเบากว่า: "Never put these sites to sleep" → เพิ่ม `game.fishbonecast.com`)
2. ปักหมุดแท็บเกม (Pin) · อย่าให้แท็บอื่นบัง · อย่าย่อหน้าต่าง Edge ทิ้งไว้
3. เปิด Edge ด้วย `--disable-features=IntensiveWakeUpThrottling,CalculateNativeWinOcclusion`
   `--disable-backgrounding-occluded-windows` `--disable-renderer-backgrounding`
   · พาธมักเป็น `C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`
   · ตรวจว่าติดจริงที่ `edge://version` → บรรทัด **Command Line**
   · ⚠️ flags ติดเฉพาะตอนเริ่มโปรเซสใหม่ — ต้องปิด Edge ให้หมดก่อน ไม่งั้นถูกทิ้งเงียบ ๆ
4. ถ้ายังตาย (`/reloads` ไม่โชว์ freeze/discard = ไม่ใช่ discard) → ต้องมี watchdog นอกเบราว์เซอร์

## 🟡 งานย่อยที่ค้าง (ไม่ด่วน)

- 🆕 **บอทตาบอดป้ายจังหวะกลางไฟต์** — ไฟต์ 08-08 12:30 ตี 118 ครั้ง บันทึกบีตได้แค่ 13
  (`📸 อ่านป้ายจังหวะไม่เจอ 15 ครั้งติด` ที่ 12:30:51 ตรงกับตอนขึ้น "เฟส 2 · โกรธแล้ว!")
  ⇒ ไม่มี feedback = ล็อกใหม่ไม่ได้ + ไม่มีข้อมูล · ต้องดูโครง DOM ของเฟส 2 จากเกมสด
- **ร้านปฏิเสธซื้อยา `❌`** เกิดบ่อย (อาจลิมิตซื้อ 3 ครั้ง/วันของเกม) — บอทควรรู้จักลิมิตแล้วเลิกลอง
- **นั่งพักพลัง ~25% ของเวลาตื่น** — พลังฟื้น ~0.55%/นาที · เป็นตัวถ่วงรายได้ (ไม่ใช่บั๊ก · ค่าที่ตั้งไว้)
- **`advThinScan`** เลือกขั้นเหยื่อที่ไม่มีของ → ไม่เก็บตัวอย่างมาหลายวัน

## ⛔ กฎที่ต้องรู้ก่อนแตะอะไร

1. **เช็คจากเกมสด ห้ามเดา** — เปิด **แท็บใหม่** เสมอ (`tabs_create_mcp`) แล้ว navigate ไป `/api/config`
   · เลือกเบราว์เซอร์ **VPS** (`Browser 2` deviceId `ecf31507-...`) ยืนยันด้วย `api.ipify.org` = `185.145.158.204`
   · **ห้าม navigate ทับแท็บที่อยู่ที่ `/play`** = แท็บบอท (v6.397 อุดรูที่แท็บ `/api/*` โหลดบอทซ้ำแล้ว แต่ยังห้ามทับ `/play`)
   · อ่าน `localStorage` คีย์ `tokpla_*` จากแท็บบอทตรง ๆ ได้ (read-only ไม่กระทบ)
2. **"ตรวจอีกรอบ" = รันเครื่องมือ** ไม่ใช่อ่าน diff — `node --check` + `node tools/check-order.js` + ชุดเทสต์
3. **ระบบล่าบอส/จับเวลา เปราะมาก** — v6.403/6.404 เดาผิด 2 รอบ กว่าจะได้ v6.405 ที่ถูก (ดูสด 22:30 แล้วค่อยแก้)
   · **ห้ามแตะ arming/`bossLeadMin` gate** (กระจายหลายจุด) — v6.405 แก้เฉพาะใน `bossTravelTo` เท่านั้น
4. **ห้ามเขียน `.user.js` ด้วย PowerShell** · CHANGELOG เขียนด้วย `.py` (`open(p,'wb')`) เท่านั้น
   · `git status` ก่อน `git checkout --` เสมอ · `git checkout --` คืนไฟล์เป็น CRLF (แปลงกลับด้วย `lf.py`)
5. **การส่งออกเพื่อแชร์ต้องตัด token** (cfgExport/diagReport) · ยกเว้น backup

## คำสั่ง Telegram ที่เพิ่มรอบนี้

`/screen` (ส่องจอกลางไฟต์) · `/angle` (มุม→บีต) · `/bosswarp on|off` · `/reloads` (โชว์ freeze/discard ด้วย)
· **`/beatcenter on|off`** (v6.408 — ปิด = กลับพฤติกรรมสมอแช่แข็งแบบ v6.407)
