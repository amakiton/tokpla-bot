# 📋 สรุปสถานะ — ส่งต่อแชทใหม่

> อัปเดตล่าสุด: 5 ส.ค. 2026 · v6.393
> อ่านไฟล์นี้ก่อน แล้วค่อยดู `CLAUDE.md` → `docs/CHANGELOG.md` (บนสุด) ตามลำดับ

## สถานะปัจจุบัน

- **v6.393** · repo สะอาด local = remote · `node tools/check-order.js` ผ่าน (410 ฟังก์ชัน)
- เทสต์ในสแครชแพด **44 ชุด ผ่านหมด**
- บอทรันบน **VPS 185.145.158.204** (คนละเครื่องกับที่แก้โค้ด) · ผู้ใช้อัปเดตเองผ่าน Tampermonkey
- ตัวเลขจริงล่าสุด: **กำไรสุทธิสะสม +4,475,696 🪙** · เหรียญในมือแตะ 2.9 ล้าน

## 🆕 v6.380 → v6.393 (รอบนี้)

| รุ่น | สิ่งที่แก้ |
|---|---|
| v6.382 | v6.379 ปลดโค้ดที่ตายมานานให้กลับมาทำงาน ⇒ **บอทเดินชนกำแพงรัว ๆ** จนคนอื่น/แอดมินสงสัย → คุมจังหวะดันประตู |
| v6.383 | เตรียมเบ็ด/เหยื่อ **ก่อนถึงเวลาบอส** ⇒ เวลาถึงนัดแรก **16 วิ → 2.1 วิ** |
| v6.384 | `bossOnlyLeadMin` — โหมดล่าบอสอย่างเดียวออกเดินเร็วขึ้น (แหล่งเดียว ไม่ต้องซิงก์ 2 ที่) |
| v6.385 | ราคาปลาใช้ **ค่าที่วัดได้จริง** (มัธยฐาน) แทนสูตรในหัว + อ่านมูลค่ากระเป๋าจากการ์ดจริง |
| v6.386–v6.388 | 🎮 **เกมอัปเดต**: ป๊อปอัพตารางบอส + ปุ่ม **"วาปไปหาบอส"** → ผูกเข้าระบบ · อ่านยอดเหรียญจริงได้แล้ว · อุดรูที่ `tools/check-order.js` (เคยตาบอดเงียบ ๆ เพราะ regex literal) |
| v6.389 | มีจุดเซฟที่ผู้ใช้บันทึกไว้ = **ไปจุดนั้นอย่างเดียว** (เลิกวนสะพาน ↔ จุดเซฟจนติดบนสะพาน) |
| v6.390 | **เปิดกระเป๋าครั้งเดียว ทำให้จบ** (เดิมเปิด-ปิดรัว ๆ ตอนเปลี่ยนเบ็ด/ทุ่น/เหยื่อ — คนอื่นเห็น) |
| v6.391 | 🥁 สมอจังหวะที่ล็อกจาก **"ดี"** ไม่มีวันถึง **"เป๊ะ"** ⇒ คอมโบ 0 ทุกไฟต์ → ไล่หา phase จนเจอ |
| v6.392 | 🎯 ตุนเหยื่อจุดอ่อน **ขั้นถูก** ให้พอ (เป้า 300 ชิ้น) + เตือนเมื่อถูกบังคับใช้ขั้นแพง |
| **v6.393** | 🛒 **"ซื้อไม่ผ่าน" ≠ "เงินไม่พอ"** — ดูด้านล่าง |

### v6.393 — ทำไมเหยื่อถึงหมดทั้งที่มีเงิน 2.9 ล้าน

    00:08:47  🎯 ตุนเหยื่อขั้น 2 เพิ่ม 2 แพ็ค → มี 50 ชิ้น (เป้า 300)
    00:08:47  🎯 ตุนเหยื่อขั้น 3 ไม่สำเร็จ — ซื้อไม่ผ่าน (เงินไม่พอ/ร้านไม่ตอบ) · มี 137 ชิ้น
    00:08:48  ❤️ เงินไม่พอซื้อยาฟื้นเลือด — เข้าถ้ำด้วยของที่มี

ทั้งสามบรรทัด **วินาทีเดียวกัน** — v6.392 ยิงซื้อติดกันไม่เว้นจังหวะ ร้านตามไม่ทัน
`buyBaitRow` คืน `true/false` ⇒ ความล้มเหลว 5 แบบถูกยุบเป็นก้อนเดียว ⇒ เลิกซื้อทั้งที่ควรลองใหม่

แก้: คืน **เหตุผล** (`ok/broke/busy/fail/locked`) · เว้น 700ms ระหว่างแพ็ค · `busy` ลองใหม่ 2 ครั้ง
· `broke` (เกมขึ้นคำว่า "เหรียญไม่พอ" เอง) เท่านั้นถึงเลิก + แจ้ง Telegram

⚠️ **`shopSettle` ห้ามใช้ Escape กู้ร้าน** — Escape ปิดร้านทั้งบาน แล้ว `shopTab` หาแท็บไม่เจอตลอดกาล

## 🔴 ปัญหาที่รู้แล้ว ยังไม่ได้แก้ (เรียงตามความสำคัญ)

### 1. เกมแพตช์ 20:09 ทำให้ `readTapFeedback()` ตาบอด

`tapNoFb` **19/23** และ **52/68** — บอทอ่านไม่ออกแล้วว่ากดแล้วได้ "เป๊ะ/ดี/หลุด"
⇒ v6.391 (ไล่หา phase) **ทำงานไม่ได้** เพราะไม่มีสัญญาณป้อนกลับ ⇒ คอมโบยังเป็น 0

- ตรวจแล้ว: ข้อความ i18n ใน bundle หลังแพตช์ **ไม่เปลี่ยน** ⇒ เกมย้าย "ที่ที่มันไปโผล่" ไม่ใช่เปลี่ยนคำ
- **ต้องได้ `/probe ตีบอส` ระหว่างไฟต์จริง** ถึงจะแก้ได้ — ห้ามเดา DOM

### 2. ระยะประชิดไม่ตรงกับเซิร์ฟเวอร์

`raidMeleeRange = 60` (เซิร์ฟเวอร์) แต่บอทใช้ ≤100 วัดจาก **จุดกึ่งกลางถึงจุดกึ่งกลาง**
ขณะที่ตัวบอสเป็นกล่อง **360×420**

log สดขัดกันเอง: ห่าง **7px** → เกมบอก "ไกลไป" · ห่าง **158px** → เกมบอก "ในระยะ"
⇒ สูตรวัดระยะของเราผิดแกน/ผิดจุดอ้างอิง · `approaches 89` vs `hits 68` ใน 101 วิ = เสียเวลาเดินเปล่า
**ต้องได้ `/probe ตีบอส` เหมือนกัน**

### 3. หน้าเกมโหลดใหม่เอง

ตัวเลขจริง **2.1 ครั้ง/ชม.** โดยเฉลี่ย แต่ **กระจุกตัว 3–10 ครั้ง/ชม. ช่วงกลางคืน**
(ตัวเลข "9 ครั้ง/ชม." ใน HANDOFF เดิมมาจากการอ่านช่วงที่กระจุกแล้วเหมาเป็นค่าเฉลี่ย — ผิด)
ถ้าไปตรงกับรอบบอส = เสียทั้งรอบ

### 4. `advThinScan` ไม่เก็บตัวอย่างมา 3 วัน

มันเลือกขั้น 11 ทุกครั้ง ซึ่ง**ไม่มีของในร้าน** ⇒ วนไม่ได้ข้อมูลสักตัว

## 🟡 รอพิสูจน์ในรอบบอสถัดไป

- v6.392/v6.393 ตุนเหยื่อขั้นถูกได้ถึงเป้า 300 ไหม (ดู `🎯 ตุนเหยื่อขั้น N เพิ่ม M แพ็ค`)
- ถ้าเห็น `🎯 ... ไม่สำเร็จ — เหรียญไม่พอ (เกมบอกเอง)` = เงินไม่พอจริง ๆ (เชื่อได้แล้ว)
- ถ้าเห็น `ร้านไม่ตอบ (ลองใหม่ 2 ครั้งแล้ว)` = ร้านมีปัญหาจริง ต้องไปดู DOM ร้าน
- v6.390 เปิดกระเป๋าครั้งเดียวจบไหม (นับจำนวนครั้งที่เปิด/ปิดใน log)

## ⛔ กฎที่ต้องรู้ก่อนแตะอะไร

0. 🔴 **การ "อ่านเฉย ๆ" ไม่ได้แปลว่าไม่กระทบ — ตรวจ `@match` ก่อนเสมอ**
   `@match *://*.fishbonecast.com/*` กินทุกพาธ รวม `/api/config` ⇒ แท็บที่เปิดไปอ่าน localStorage
   ก็โหลดบอทอีกตัว · ปิดแท็บ = `pagehide` → `persistEnabled(enabled=false)` → **ลบธง resume**
   ⇒ แท็บบอทตัวจริงโหลดใหม่แล้วไม่เปิดต่อ = **ตายเงียบ 106 นาที พลาดบอส 2 รอบ (5 ส.ค.)**
   **v6.397 อุดแล้ว** แต่บทเรียนยังอยู่: เครื่องมือวินิจฉัยที่รันในระบบเดียวกับที่ถูกวินิจฉัย
   คือส่วนหนึ่งของระบบ

1. **เช็คจากเกมสด ห้ามเดา** — เปิด **แท็บใหม่** เสมอ (`tabs_create_mcp`) แล้ว navigate ไป `/api/config`
   · **ห้าม navigate ทับแท็บที่อยู่ที่ `/play`** = แท็บบอท (เคยพลาด บอทตาย กู้เองไม่ได้)
   · เลือกเบราว์เซอร์ของ **VPS** (`Browser 2`) แล้วยืนยันด้วย `api.ipify.org` = `185.145.158.204`
   · **ห้ามเปิดแท็บช่วงใกล้เวลาบอส/ระหว่างไฟต์** (อาจดึงโฟกัส → เทสต์คุมตัวละครไม่ผ่าน → ครบ 3 ครั้งข้ามรอบ)
2. **"ตรวจอีกรอบ" = รันเครื่องมือ** ไม่ใช่อ่าน diff — `node --check` + **`node tools/check-order.js`** + ชุดเทสต์
3. **ข้อความที่ตาเห็นบนจอ ≠ `textContent`** — v6.387 สร้างจากภาพหน้าจอแล้วผิด (อิโมจิที่เห็นไม่ได้อยู่ในปุ่ม)
   ⇒ ต้องได้ `/probe <ชื่อปุ่ม>` จริงก่อนผูกโค้ดกับข้อความ
4. **การแก้บั๊กที่ทำให้สาขาโค้ดที่ตายอยู่กลับมาทำงาน = การปล่อยโค้ดที่ไม่มีใครรีวิว** (บทเรียน v6.379 → v6.382)
5. **ฟีเจอร์ที่เก็บข้อมูลเอง ยังไม่เสร็จจนกว่าจะเห็นข้อมูลจริงบน VPS** (พลาดมาแล้ว 3 รอบ)
6. **ห้ามเขียนไฟล์ `.user.js` ด้วย PowerShell** (`Set-Content -Encoding utf8` เคยทำทั้งไฟล์เป็นตัวยึกยือ)
   · เขียน CHANGELOG ด้วยไฟล์ `.py` (`open(p,'wb')`) เท่านั้น อย่าพิมพ์ python ในเชลล์ (โดน backtick กิน)
   · **`git status` ก่อน `git checkout --` เสมอ** (เคยล้างงานที่ยังไม่ commit ทิ้งทั้งเวอร์ชัน)
   · `git checkout --` คืนไฟล์เป็น CRLF → เทสต์ที่ค้นหา `\n` พังเงียบ ๆ (แปลงกลับด้วย `lf.py`)

## 🔵 งานที่ยังไม่เริ่ม

- **`/probe ตีบอส` ระหว่างไฟต์จริง** → แก้ข้อ 1 + ข้อ 2 ข้างบนพร้อมกัน (คุ้มที่สุดตอนนี้)
- **ปุ่มเหยื่อ 2 ปุ่มในถ้ำ** → แก้ `currentBait()` ให้อ่านปุ่มที่ใส่อยู่จริง (รอ `/baitbtn`)
- **รีโหลดเอง** (ดูข้อ 3)
- **`advThinScan` เลือกขั้นที่ไม่มีของ** (ดูข้อ 4)

## คำสั่ง Telegram ที่ใช้บ่อย

`/bossstats` · `/bossstats ev` · `/bosslog` · `/boss` · `/log 40` · `/probe <ชื่อปุ่ม>`
`/today` · `/gear` · `/gap` · `/bosstime` · `/baitbtn` · `/xp` · `/xpnow` · `/xpprobe`
